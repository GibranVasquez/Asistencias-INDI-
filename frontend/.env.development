# Solo se carga con `electron-vite dev` (modo development), NUNCA con
# `electron-vite build` (modo production) — por eso el modo de prueba del
# kiosco no puede colarse en un build real solo por olvido.
VITE_ENABLE_MODO_PRUEBA=true
