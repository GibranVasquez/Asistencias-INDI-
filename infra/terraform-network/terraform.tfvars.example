# Copiar a terraform.tfvars (gitignoreado) y llenar antes de aplicar este
# stack - ver infra/AWS_MIGRATION.md.

aws_region = "us-east-1"

# Opcionales - ya tienen defaults razonables, sobreescribir solo si aplica:
# project_name = "indi-asistencia"
# environment  = "production"
# vpc_cidr     = "10.0.0.0/16"
